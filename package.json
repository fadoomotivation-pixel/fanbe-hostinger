{
  "name": "fanbe-hostinger",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/copy-main-website.mjs",
    "deploy": "npm run build && node scripts/ftp-deploy.mjs"
  },
  "devDependencies": {
    "basic-ftp": "^5.0.5",
    "dotenv": "^16.4.5"
  }
}
